<h2 class="text-center mt-4"><%= @article.title %></h1>
  <div class="container">
    <div class="row justify-content-center">
    <div class="col-8 mt-4 mb-4">
      <div class="card text-center shadow mb-5 bg-white rounded">
        <div class="card-header font-italic">
           by <%= @article.user.username if @article.user %>
           <%= link_to gravatar_for(@article.user, size: 80), user_path(@article.user) %>
           <% if @article.categories.any? %>
            <div class="mt-2"><%= render @article.categories %></div>
           <% end %>
        </div>
        <div class="card-body">
          <div class="card-text text-left"><%= simple_format(@article.description) %></div>
          <% if logged_in? && (@article.user == current_user || current_user.admin?) %>
<<<<<<< HEAD
              <%= link_to 'Edit', edit_article_path(@article), class: "btn btn-outline-info mr-1" %>
=======
            <%= link_to 'Edit', edit_article_path(@article), class: "btn btn-outline-info" %>
            <%= link_to 'Delete', article_path(@article),class: "btn btn-outline-danger", method: :delete, data: {confirm: "Are you sure?" } %>
>>>>>>> 84b142e2bd19d2435e3b97a24318422dca21b2e9
          <% end %>
        </div>
        <div class="card-footer text-muted">
        <small>Created <%= time_ago_in_words(@article.created_at)%> ago, <%= time_ago_in_words(@article.created_at)%> edited ago</small>
        </div>
      </div>
      </div>
    </div>
    <div class="form-group row justify-content-center">
      <%= link_to 'Add new', new_article_path %> |
      <%= link_to 'Home', root_path %>
    </div>
  </div>
  </div>
 </div>
